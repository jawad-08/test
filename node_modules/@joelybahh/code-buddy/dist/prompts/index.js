var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import chalk from "chalk";
import inquirer from "inquirer";
import { applyConfigTransform, applyConfigTransformAsync, applyEmoji, applyIssueKey, applyScopeTrim, applySentenceCase, isTruthy, } from "../utils/config.js";
import { commitAll, getDiffForFiles } from "../utils/git.js";
import { loadConfig } from "../utils/openai.js";
function scopeConfirmationPrompt(scope) {
    return __awaiter(this, void 0, void 0, function* () {
        const confirmScope = [
            {
                type: "confirm",
                name: "confirmScope",
                message: `Would you like to commit all changes in ${scope}?`,
                default: true,
            },
        ];
        const confirmScopeAnswers = yield inquirer.prompt(confirmScope);
        return confirmScopeAnswers.confirmScope;
    });
}
function commitConfirmationPrompt(message) {
    var _a, _b, _c, _d, _e, _f, _g;
    return __awaiter(this, void 0, void 0, function* () {
        const config = yield loadConfig();
        // Extract config values
        const issue = (_a = config === null || config === void 0 ? void 0 : config.commit) === null || _a === void 0 ? void 0 : _a.issue;
        const sentenceCase = (_c = (_b = config === null || config === void 0 ? void 0 : config.commit) === null || _b === void 0 ? void 0 : _b.format) === null || _c === void 0 ? void 0 : _c.sentenceCase;
        const scopeTrim = (_d = config === null || config === void 0 ? void 0 : config.commit) === null || _d === void 0 ? void 0 : _d.scopeTrim;
        const emoji = (_f = (_e = config === null || config === void 0 ? void 0 : config.commit) === null || _e === void 0 ? void 0 : _e.format) === null || _f === void 0 ? void 0 : _f.useEmoji;
        // Apply config transforms
        // prettier-ignore
        message = yield applyConfigTransformAsync(message, isTruthy(issue), applyIssueKey, (_g = config.commit) === null || _g === void 0 ? void 0 : _g.issue);
        message = applyConfigTransform(message, isTruthy(sentenceCase), applySentenceCase);
        message = applyConfigTransform(message, isTruthy(scopeTrim), applyScopeTrim, scopeTrim);
        message = applyConfigTransform(message, isTruthy(emoji), applyEmoji, emoji);
        console.log(chalk.green("✅ Successfully Generated \n\n") + chalk.bold.yellow(message) + "\n");
        // Ask user if they want to use the generated commit message
        const confirmCommitMessage = [
            {
                type: "confirm",
                name: "confirmCommitMessage",
                message: "Would you like to use it?",
                default: true,
            },
        ];
        const confirmCommitMessageAnswers = yield inquirer.prompt(confirmCommitMessage);
        return [message, confirmCommitMessageAnswers.confirmCommitMessage];
    });
}
function diffSizePrompt(diff, files) {
    return __awaiter(this, void 0, void 0, function* () {
        const config = yield loadConfig();
        if (config.diff.exclude) {
            files = files.filter((file) => !config.diff.exclude.includes(file));
            diff = yield getDiffForFiles(files);
        }
        if (diff.length < config.diff.maxSize)
            return diff;
        const selectQuestions = [
            {
                type: "checkbox",
                name: "files",
                message: chalk.red(`❌ The diff is too large to generate a commit message (length ${diff.length}). Please select the files you want to use in the commit message.`),
                choices: files,
            },
        ];
        const filesSelected = yield inquirer.prompt(selectQuestions);
        return yield getDiffForFiles(filesSelected.files);
    });
}
export function commitAllPrompts(...args) {
    return __awaiter(this, void 0, void 0, function* () {
        const [optionalArgs] = args;
        yield commitAll(scopeConfirmationPrompt, commitConfirmationPrompt, diffSizePrompt, optionalArgs);
    });
}
