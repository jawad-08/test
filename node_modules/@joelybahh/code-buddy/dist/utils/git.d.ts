import { CommitResult } from "simple-git";
declare enum StagedFileType {
    AllExceptDeleted = "allExceptDeleted",
    Modified = "modified",
    Deleted = "deleted",
    All = "all"
}
/**
 * Get staged files.
 * @param {string} scope - The scope to group files by.
 * @returns {Promise<string[]>} A promise that resolves to an array of staged files.
 */
export declare function getStagedFiles(type?: StagedFileType, scope?: string): Promise<string[]>;
/**
 * Get issue key from branch name.
 * @param {RegExp} regexp - The regular expression to match the issue key.
 * @returns {Promise<string | undefined>} A promise that resolves to the issue key or undefined.
 */
export declare function getIssueKeyFromBranchName(regexp?: RegExp): Promise<string | undefined>;
/**
 * Get diff for specific files.
 * @param {string[]} files - The files to get the diff for.
 * @returns {Promise<string>} A promise that resolves to the diff for the specified files.
 */
export declare function getDiffForFiles(files: string[]): Promise<string>;
/**
 * Commit changes.
 * @param {string} message - The commit message.
 * @returns {Promise<CommitResult | undefined>} A promise that resolves to the result of git.commit or undefined.
 */
export declare function commitChanges(message: string): Promise<CommitResult | undefined>;
import { OptionalArgs } from "../types/index.js";
/**
 * Group files by scope.
 * @param {string[]} files - The files to group.
 * @returns {Promise<Record<string, string[]>>} An object mapping scopes to arrays of files.
 */
export declare function groupFilesByScope(files: string[]): Promise<Record<string, string[]>>;
/**
 * Main function to handle commit.
 * @param {string} diff - The changes to commit.
 * @param {string} scope - The scope of changes.
 * @returns {Promise<void>} Nothing.
 */
export declare function commit(diff: string, scope: string, confirmCommit: (message: string) => Promise<[string, boolean]>, args: OptionalArgs): Promise<void>;
/**
 * Main function to handle commit-all.
 * @param {string[]} diff - The changes to commit.
 * @param {string} scope - The scope of changes.
 * @returns {Promise<void>} Nothing.
 */
export declare function commitAll(confirmScope: (scope: string) => Promise<boolean>, confirmCommit: (message: string) => Promise<[string, boolean]>, reduceDiff: (diff: string, files: string[]) => Promise<string>, args: OptionalArgs): Promise<void>;
export {};
