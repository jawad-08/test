import { CodeBuddyConfig } from "../types/index.js";
/**
 *
 * @param message
 * @param configProp
 * @param transformFunc
 * @param additionalArg
 * @returns
 */
export declare function applyConfigTransform(message: string, configProp: boolean | string, transformFunc: (message: string, prop?: any) => string, additionalArg?: any): string;
/**
 *
 * @param message
 * @param configProp
 * @param transformFunc
 * @param additionalArg
 * @returns
 */
export declare function applyConfigTransformAsync(message: string, configProp: boolean | string, transformFunc: (message: string, prop?: any) => Promise<string>, additionalArg?: any): Promise<string>;
/**
 * Applies the issue key to the commit message.
 * @param message The commit message
 * @param issueConfig The issue config from the CodeBuddy config
 */
export declare function applyIssueKey(message: string, issueConfig: CodeBuddyConfig["commit"]["issue"]): Promise<string>;
/**
 * Applies sentence case to a commit message
 * @param message The message to apply sentence case to
 */
export declare function applySentenceCase(message: string): string;
/**
 * Applies an emoji to a commit message based on the commit type extracted from the message
 * @param message The commit message
 * @returns The commit message with the emoji applied
 */
export declare function applyEmoji(message: string): string;
/**
 * Performs a trim on the message scope, based on the trim
 *
 * @param message The commit message
 * @param trim The string to trim from the scope
 *
 * @example
 * ```ts
 * // returns "feat(scope): my message"
 * applyScopeTrim("feat(inspace-scope): my message", "inspace-")
 * ```
 * @returns The commit message with the scope trimmed
 */
export declare function applyScopeTrim(message: string, trim: string): string;
/**
 * A wrapper method that checls if a value is truthy
 *
 * @param value The value to check
 * @returns A boolean indicating if the value is truthy
 *
 * @remarks This method is purely for readability
 */
export declare function isTruthy(value: any): boolean;
