/**
 * The configuration for CodeBuddy.
 */
export type CodeBuddyConfig = {
    chatGPT?: {
        apiKey: string;
        organization: string;
        model: "gpt-4" | "gpt-3.5-turbo";
        maxTokens?: number;
        temperature?: number;
        topP?: number;
        frequencyPenalty?: number;
        presencePenalty?: number;
        stop?: string[];
    };
    commit?: {
        scopeTrim: string;
        issue?: {
            detectKey?: boolean;
            keyRegex?: RegExp;
            fallbackKey?: string;
        };
        format?: {
            sentenceCase?: boolean;
            useEmoji?: boolean;
        };
        scope?: {
            mode: ScopeMode;
            srcDir?: string;
        };
    };
    diff?: {
        maxSize?: number;
        exclude?: string[];
    };
};
/**
 * The scope mode to use when generating commit messages.
 */
export declare enum ScopeMode {
    Monorepo = "monorepo",
    Traditional = "traditional"
}
/**
 * An enum representing the different types of commits.
 */
export declare enum CommitType {
    Feature = "feat",
    Fix = "fix",
    Docs = "docs",
    Style = "style",
    Refactor = "refactor",
    Perf = "perf",
    Test = "test",
    Chore = "chore",
    Ci = "ci",
    Build = "build",
    Revert = "revert"
}
/**
 * A string literal type representing the different types of commits.
 */
export type TCommitType = CommitType.Feature | CommitType.Fix | CommitType.Docs | CommitType.Build | CommitType.Style | CommitType.Refactor | CommitType.Perf | CommitType.Test | CommitType.Chore | CommitType.Ci | CommitType.Revert;
/**
 * A record mapping commit types to their emoji.
 */
export declare const COMMIT_TYPE_EMOJIS: Record<CommitType, string>;
/**
 * A type representing the optional arguments for the `commit-all` command.
 */
export type OptionalArgs = {
    breaking?: boolean;
    message?: string;
    scope?: string;
    issue?: string;
    type?: TCommitType;
    reason?: string;
};
export declare class ConfigLoadError extends Error {
    constructor(message: string);
}
export declare class CommitMessageError extends Error {
    constructor(message: string);
}
